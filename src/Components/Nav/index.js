import React from "react";

import "./style.css";

export default function Nav({ currentPage, handlePageChange}) {
  return (
    <footer className="nav">
      <h1>magellan</h1>
    </footer>
  );
}
